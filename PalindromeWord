// palendrome word finder program

#include <iostream>
#include <string>
using namespace std;

int main() {
	string word1;
	int num;
	
	cout << "Please enter a word: ";
	cin  >> word1;
	
	cout << "Last character: " << word1.at(word1.size() - 1) << endl;
	cout << "First character: " << word1.at(0) << endl;
	
	if (word1.at(word1.size() - 1) == word1.at(0))
		cout << "the word " << word1 << " is a palendrome.\n";
	else
		cout << "the word " << word1 << " is not a panlendrome. \n";
	
	return(0);
}
